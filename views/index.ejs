<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Welcome Back to Work</title>

  <!-- Google Font -->
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap"
    rel="stylesheet"
  />

  <!-- Bootstrap & Icons -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    rel="stylesheet"
  />

  <style>
    :root {
      --theme: rgb(16,239,90);
      --theme-dark: rgb(10,180,70);
    }
    body {
      font-family: 'Inter', sans-serif;
      background: #f4f7fa;
    }
    .hero {
      background: linear-gradient(135deg, var(--theme) 0%, var(--theme-dark) 100%);
      color: #fff;
      padding: 6rem 1rem 4rem;
      text-align: center;
      margin-bottom: 2rem;
    }
    .rp-logo {
      max-width: 140px;
      margin-bottom: 1.5rem;
      filter: drop-shadow(0 4px 8px rgba(0,0,0,0.3));
    }
    .hero h1 {
      font-weight: 700;
      font-size: 2.75rem;
      margin-bottom: 0.5rem;
    }
    .hero p {
      font-size: 1.15rem;
      opacity: 0.9;
      margin-bottom: 2rem;
    }

    .btn-hero {
      display: inline-flex;
      align-items: center;
      gap: .5rem;
      padding: .75rem 2rem;
      font-size: 1.25rem;
      font-weight: 600;
      border-radius: 50px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.15);
      transition: transform .2s, box-shadow .2s;
    }
    .btn-hero.btn-solid {
      background: var(--theme);
      color: #fff;
      border: none;
    }
    .btn-hero.btn-outline {
      background: #fff;
      color: var(--theme);
      border: 3px solid var(--theme);
    }
    .btn-hero:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 25px rgba(0,0,0,0.2);
    }

    .campus-banner {
      position: relative;
      overflow: hidden;
      border-radius: 1rem;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
      margin: 2rem 1rem;
    }
    .campus-banner img {
      width: 100%;
      height: 350px;
      object-fit: cover;
      filter: brightness(0.7);
    }
    .campus-banner .overlay {
      position: absolute;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(16,239,90,0.4);
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .campus-banner h2 {
      color: #fff;
      font-size: 2rem;
      font-weight: 600;
      text-shadow: 2px 2px 6px rgba(0,0,0,0.6);
      margin: 0;
      padding: 0 1rem;
      text-align: center;
    }

    footer {
      text-align: center;
      padding: 1.5rem;
      color: #666;
      font-size: .9rem;
    }
    footer a {
      color: var(--theme);
      text-decoration: none;
      margin: 0 .5rem;
    }
    footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
<!--BIG BIG BANNER-->
  <section class="hero">
    <img src="images/rp-logo.png" alt="Republic Polytechnic Logo" class="rp-logo" />
    <% if (user) { %>
      <h1>Welcome Back to Work!</h1>
      <p>Your time is valuable—punch in and out with ease.</p>
    <% } else { %>
      <h1>Welcome Back to Work!</h1>
      <p>Register or log in to get started tracking your hours.</p>
      <a href="/register" class="btn-hero btn-outline">
        <i class="bi bi-pencil-square"></i> Register
      </a>
      <a href="/login" class="btn-hero btn-solid ms-3">
        <i class="bi bi-box-arrow-in-right"></i> Login
      </a>
    <% } %>
  </section>

  <!-- CAMPUS BANNER -->
  <section class="campus-banner">
    <img src="images/rp-campus.png" alt="Republic Polytechnic Campus">
    <div class="overlay">
      <% if (user) { %>
        <h2>Empowering RP Students, One Hour at a Time</h2>
      <% } else { %>
        <h2>Empowering RP Students, One Hour at a Time</h2>
      <% } %>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    &copy; <%= new Date().getFullYear() %> Republic Polytechnic  
    <a href="#">Privacy Policy</a> | <a href="#">Terms of Use</a>
  </footer>

</body>
</html>

