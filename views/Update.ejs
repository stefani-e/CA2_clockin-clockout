<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Welcome to our website</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Welcome to our website</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/Add">Add new timesheet</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/List">View timesheet</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Timesheet</h2></div>
        <br>
        <div class="text-center">
          <% if (timesheet) { %> 
              <form action="/update/<%= user.staff_id %>" method="POST" enctype="multipart/form-data">
                  <label for="Clock_in">Clock-in time:</label><br>
                  <input type="number" id="Clock_in" name="Clock_in" min="0" step="1" value="<%= user["Clock_in"] %>" required><br><br>

                  <label for="Clock_out">Clock-out time:</label><br>
                  <input type="number" id="Clock_out" name="Clock_out" min="0" step="1" value="<%= user["Clock_out"] %>" required><br><br>

                  <label for="Break_start">Break Start:</label><br>
                  <input type="number" id="Break_start" name="Break_start" min="0" step="0.01" value="<%= user["Break_start"] %>" required><br><br>

                  <label for="Break_end">Break End:</label><br>
                  <input type="number" id="Break_end" name="Break_end" min="0" step="0.01" value="<%= user["Break_end"] %>" required><br><br>

                  <label for="Total_hour">Total hours:</label><br>
                  <input type="number" id="Total_hour" name="Total_hour" min="0" step="0.01" value="<%= user["Total_hour"] %>" required><br><br>

                  <button type="submit" class="btn btn-primary">Update Timesheet</button>
              </form>
            <% } else { %>
                <p>No timesheet found.</p>
            <% } %>

          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
